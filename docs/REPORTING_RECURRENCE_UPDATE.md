# Обновление отчетности: Периодические задачи

Дата: 27 ноября 2025

## Что добавлено

Добавлена функциональность **периодических задач** в раздел отчетности по чек-листам из родительского проекта toplevel.

## Изменения в базе данных

### Новые поля в модели `ReportingTask`

```prisma
model ReportingTask {
  // ... существующие поля ...
  
  // Поля для периодичности
  isRecurring       Boolean               @default(false)
  frequency         String?               // "DAILY" или "WEEKLY"
  weekDay           Int?                  // 0-6 (воскресенье-суббота) для еженедельных задач
  stoppedAt         DateTime?             // Дата остановки повторения
  parentTaskId      String?               // ID родительской задачи для повторяющихся
  
  @@index([isRecurring])
  @@index([parentTaskId])
}
```

### Миграция

- Выполнена миграция: `npx prisma db push`
- Все существующие задачи получили `isRecurring = false` по умолчанию
- Добавлены индексы для оптимизации запросов

## Новые файлы

### 1. `src/lib/reporting-virtual-tasks.ts`

Библиотека для работы с виртуальными повторяющимися задачами.

**Основные функции:**
- `shouldCreateTaskForDate()` - определяет, нужно ли создать виртуальную задачу на дату
- `generateVirtualTasks()` - генерирует виртуальные задачи для периода
- `getTasksWithVirtual()` - получает реальные и виртуальные задачи

**Как работает:**
- Для повторяющихся задач создаются "виртуальные" экземпляры на каждый день/неделю
- Виртуальные задачи имеют флаг `isVirtual: true`
- ID виртуальной задачи: `{parentTaskId}-{date}`
- При завершении виртуальной задачи создается реальная запись в БД

### 2. `src/app/api/reporting/tasks/[id]/stop/route.ts`

API endpoint для остановки повторяющихся задач.

**Метод:** `POST /api/reporting/tasks/{id}/stop`

**Доступ:** Только ADMIN и DEPUTY_ADMIN

**Что делает:**
- Устанавливает `stoppedAt = текущая дата`
- После этого виртуальные задачи больше не генерируются

## Обновленные файлы

### 1. `src/app/api/reporting/objects/[id]/tasks/route.ts`

**Изменения:**
- Использует `getTasksWithVirtual()` для получения задач
- Поддерживает фильтрацию по дате (`date` параметр)
- Возвращает как реальные, так и виртуальные задачи

**Новые параметры API:**
```typescript
GET /api/reporting/objects/{id}/tasks?date=2025-11-27
```

### 2. `src/components/ReportingObjectDetail.tsx`

**Новые возможности:**

#### Создание повторяющейся задачи
- Чекбокс "Повторяющаяся задача"
- Выбор периодичности: Ежедневно / Еженедельно
- Для еженедельных: выбор дня недели (Пн-Вс)

#### Навигация по дням
- Кнопки "← Предыдущий день" и "Следующий день →"
- Отображение текущей выбранной даты
- Автоматическая загрузка задач для выбранной даты

#### Индикация повторяющихся задач
- Иконка 🔄 (Repeat) для повторяющихся задач
- Бейдж с информацией о периодичности
- Кнопка "Остановить повторение" для админов

#### Остановка повторения
- Кнопка "🛑 Остановить" для повторяющихся задач
- Подтверждение действия
- После остановки задача больше не создается автоматически

## Как использовать

### Создание ежедневной задачи

1. Откройте объект отчетности
2. Нажмите "Создать задачу"
3. Заполните название и описание
4. Включите "Повторяющаяся задача"
5. Выберите "Ежедневно"
6. Создайте задачу

**Результат:** Задача будет автоматически появляться каждый день

### Создание еженедельной задачи

1. Откройте объект отчетности
2. Нажмите "Создать задачу"
3. Заполните название и описание
4. Включите "Повторяющаяся задача"
5. Выберите "Еженедельно"
6. Выберите день недели (например, "Понедельник")
7. Создайте задачу

**Результат:** Задача будет появляться каждую неделю в выбранный день

### Навигация по дням

1. Откройте объект отчетности
2. Используйте кнопки "←" и "→" для переключения между днями
3. Задачи автоматически обновятся для выбранной даты

### Остановка повторения

1. Найдите повторяющуюся задачу (с иконкой 🔄)
2. Нажмите кнопку "🛑 Остановить"
3. Подтвердите действие
4. Задача больше не будет создаваться автоматически

## Технические детали

### Виртуальные задачи

Виртуальные задачи не хранятся в базе данных до момента их завершения. Они генерируются "на лету" при запросе задач для конкретной даты.

**Преимущества:**
- Не засоряют базу данных
- Легко изменить параметры повторения
- Быстрая работа

**ID виртуальной задачи:**
```
{parentTaskId}-2025-11-27
```

### Логика генерации

```typescript
// Для ежедневных задач
if (frequency === 'DAILY') {
  // Создается задача на каждый день после даты создания
}

// Для еженедельных задач
if (frequency === 'WEEKLY' && weekDay === targetWeekDay) {
  // Создается задача только в указанный день недели
}

// Проверка остановки
if (stoppedAt && date >= stoppedAt) {
  // Задачи не создаются после даты остановки
}
```

### API Endpoints

#### Получение задач с виртуальными
```
GET /api/reporting/objects/{objectId}/tasks?date=2025-11-27
```

**Ответ:**
```json
{
  "tasks": [
    {
      "id": "real-task-id",
      "isVirtual": false,
      "isRecurring": true,
      "frequency": "DAILY",
      ...
    },
    {
      "id": "parent-id-2025-11-27",
      "isVirtual": true,
      "parentTaskId": "parent-id",
      "scheduledDate": "2025-11-27T00:00:00.000Z",
      ...
    }
  ]
}
```

#### Остановка повторения
```
POST /api/reporting/tasks/{taskId}/stop
```

**Ответ:**
```json
{
  "message": "Повторение задачи остановлено",
  "task": {
    "id": "task-id",
    "stoppedAt": "2025-11-27T10:30:00.000Z",
    ...
  }
}
```

## Совместимость

- ✅ Все существующие задачи работают как раньше
- ✅ Новые поля опциональны
- ✅ Обратная совместимость полностью сохранена
- ✅ Миграция не требует изменения данных

## Тестирование

### Проверка создания ежедневной задачи

1. Создайте ежедневную задачу
2. Переключитесь на завтрашний день
3. Убедитесь, что задача появилась
4. Переключитесь на послезавтра
5. Убедитесь, что задача снова появилась

### Проверка создания еженедельной задачи

1. Создайте еженедельную задачу на понедельник
2. Переключитесь на следующий понедельник
3. Убедитесь, что задача появилась
4. Переключитесь на вторник
5. Убедитесь, что задачи нет

### Проверка остановки

1. Создайте ежедневную задачу
2. Остановите её
3. Переключитесь на завтрашний день
4. Убедитесь, что новая задача НЕ появилась

## Источник

Все изменения взяты из родительского проекта **toplevel**, коммит `9a41c01`:
```
commit 9a41c01c7ebd6434384d066b01a631a40b2f40b5
Author: olgakycenko <olgakucenko97@gmail.com>
Date:   Sat Nov 22 00:05:37 2025 +0300

    Отчетность: периодические задачи, навигация по дням и виртуальные повторения
```

## Следующие шаги (опционально)

1. Добавить возможность редактирования параметров повторения
2. Добавить уведомления о новых виртуальных задачах
3. Добавить статистику по повторяющимся задачам
4. Добавить экспорт повторяющихся задач

---

**Статус:** ✅ Все изменения применены и готовы к использованию
