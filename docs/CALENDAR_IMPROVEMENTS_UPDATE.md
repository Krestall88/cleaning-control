# Обновление календаря: Массовое закрытие и индикаторы требований

Дата: 27 ноября 2025

## Что добавлено

Добавлены улучшения календаря из родительского проекта toplevel:
1. **Массовое закрытие задач** с выбором конкретных задач
2. **Индикаторы требований** к закрытию задач (фото/комментарии)
3. **Улучшенная загрузка фотографий** при завершении задач
4. **Отображение статусов** в карточках задач

## Новые возможности

### 1. Массовое закрытие задач

#### Режим выбора задач
- Кнопка "Выбрать задачи" в модальном окне списка задач
- Чекбоксы для выбора конкретных задач
- Счетчик выбранных задач
- Возможность выбрать все или снять выбор

#### Умное закрытие
- Автоматически пропускает задачи, требующие фото или комментарии
- Закрывает только те задачи, которые можно закрыть без дополнительных данных
- Показывает количество закрытых и пропущенных задач

#### Два режима работы:
1. **Без выбора** - закрывает все просроченные и сегодняшние задачи
2. **С выбором** - закрывает только выбранные задачи

### 2. Индикаторы требований в карточках объектов

#### Визуальные индикаторы
- 📷 **Камера** - требуется фото при закрытии
- 💬 **Комментарий** - требуется комментарий при закрытии
- 🛡️ **Оба** - требуется и фото, и комментарий

#### Цветовая кодировка
- **Красный** - требуется фото и комментарий (строгие требования)
- **Оранжевый** - требуется фото
- **Синий** - требуется комментарий
- **Зеленый** - нет требований (можно закрыть быстро)

#### Анимация
- Пульсирующая анимация для объектов с требованиями
- Привлекает внимание к важным требованиям

### 3. Улучшенная загрузка фотографий

#### Множественная загрузка
- Загрузка нескольких фото одновременно
- Предпросмотр загруженных фото
- Возможность удалить фото до отправки

#### Оптимизация
- Сжатие изображений перед загрузкой
- Прогресс-бар загрузки
- Обработка ошибок загрузки

### 4. Улучшенное отображение статусов

#### Статусы задач
- ✅ **Выполнено** (зеленый)
- ⏰ **На сегодня** (синий)
- ⚠️ **Просрочено** (красный)
- 📅 **Предстоящая** (серый)

#### Группировка в модальном окне
- Вкладка "Просроченные"
- Вкладка "На сегодня"
- Вкладка "Выполненные"

## Новые файлы

### 1. `src/app/api/tasks/bulk-complete/route.ts`

API endpoint для массового закрытия задач.

**Метод:** `POST /api/tasks/bulk-complete`

**Параметры:**
```typescript
{
  taskIds: string[];           // Массив ID задач для закрытия
  skipValidation?: boolean;    // Пропустить валидацию требований
}
```

**Ответ:**
```typescript
{
  message: string;             // Сообщение о результате
  completed: number;           // Количество закрытых задач
  skipped: number;            // Количество пропущенных задач
  skippedTasks: Array<{       // Детали пропущенных задач
    id: string;
    title: string;
    reason: string;
  }>;
}
```

**Логика работы:**
1. Проверяет авторизацию пользователя
2. Получает список задач для закрытия
3. Для каждой задачи:
   - Проверяет, не закрыта ли уже
   - Проверяет требования объекта (фото/комментарий)
   - Если требования есть - пропускает
   - Если нет требований - закрывает
4. Материализует виртуальные задачи при необходимости
5. Возвращает статистику

### 2. `src/app/api/photos/[id]/route.ts`

API для работы с фотографиями задач.

**Методы:**
- `GET /api/photos/{id}` - получить фото
- `DELETE /api/photos/{id}` - удалить фото

**Улучшения:**
- Поддержка Supabase Storage
- Оптимизированная загрузка
- Обработка ошибок

## Обновленные файлы

### 1. `src/components/SimpleTaskListModal.tsx`

**Новые возможности:**

#### Режим выбора задач
```typescript
const [selectionMode, setSelectionMode] = useState(false);
const [selectedTasks, setSelectedTasks] = useState<Set<string>>(new Set());
```

#### Кнопка массового закрытия
- Показывается, если есть незакрытые задачи
- Учитывает требования объектов
- Отображает количество выбранных задач

#### UI элементы
- Чекбоксы для выбора задач
- Кнопка "Выбрать задачи" / "Отменить выбор"
- Счетчик выбранных задач
- Предупреждение о требованиях

### 2. `src/components/ObjectCard.tsx`

**Новые возможности:**

#### Функция определения требований
```typescript
const getCompletionRequirements = () => {
  const hasPhoto = object.requirePhotoForCompletion;
  const hasComment = object.requireCommentForCompletion;
  
  if (hasPhoto && hasComment) {
    return { 
      icon: Shield, 
      text: 'Фото + Комментарий', 
      color: 'bg-red-100 text-red-800 border-red-300' 
    };
  }
  // ... другие варианты
}
```

#### Визуальный индикатор
- Отображается под информацией о менеджере
- Пульсирует, если есть требования
- Показывает иконку и текст требований

### 3. `src/components/UnifiedTaskCompletionModal.tsx`

**Улучшения:**

#### Множественная загрузка фото
- Поддержка `multiple` атрибута
- Предпросмотр всех загруженных фото
- Удаление отдельных фото

#### Улучшенный UI
- Сетка фотографий 2x2
- Кнопки удаления на каждом фото
- Индикатор загрузки

#### Валидация
- Проверка обязательности фото
- Проверка обязательности комментария
- Предупреждения пользователю

### 4. `src/lib/unified-task-system.ts`

**Новые функции:**

#### `materializeVirtualTask()`
Материализует виртуальную задачу в реальную запись БД.

```typescript
export async function materializeVirtualTask(
  virtualTaskId: string,
  completedById: string,
  completionNote?: string,
  completionPhotos?: string[]
): Promise<Task>
```

**Использование:**
- Вызывается при закрытии виртуальной задачи
- Создает реальную запись в БД
- Сохраняет все данные о завершении

### 5. `src/app/api/tasks/unified-complete/route.ts`

**Улучшения:**

#### Поддержка виртуальных задач
- Автоматически материализует виртуальные задачи
- Сохраняет фотографии
- Обрабатывает комментарии

#### Валидация
- Проверяет требования объекта
- Валидирует наличие фото/комментария
- Возвращает понятные ошибки

### 6. `src/app/api/tasks/calendar-unified/route.ts`

**Улучшения:**

#### Оптимизация запросов
- Использует `select` для выборки только нужных полей
- Добавлены индексы для быстрого поиска
- Кэширование результатов

#### Поддержка фильтров
- Фильтрация по статусу
- Фильтрация по периодичности
- Фильтрация по менеджеру

## Как использовать

### Массовое закрытие задач

#### Вариант 1: Закрыть все доступные задачи

1. Откройте календарь
2. Нажмите на карточку объекта
3. В модальном окне нажмите "Закрыть все задачи"
4. Подтвердите действие
5. Система закроет все задачи без требований

#### Вариант 2: Выбрать конкретные задачи

1. Откройте календарь
2. Нажмите на карточку объекта
3. Нажмите "Выбрать задачи"
4. Отметьте нужные задачи чекбоксами
5. Нажмите "Закрыть выбранные (N)"
6. Подтвердите действие

### Просмотр требований объекта

1. Откройте календарь
2. Найдите карточку объекта
3. Посмотрите на индикатор под именем менеджера:
   - 🛡️ **Красный** - нужно фото И комментарий
   - 📷 **Оранжевый** - нужно только фото
   - 💬 **Синий** - нужен только комментарий
   - ✅ **Зеленый** - ничего не нужно

### Загрузка нескольких фото

1. Откройте задачу для завершения
2. Нажмите "Выбрать фото"
3. Выберите несколько файлов (Ctrl+Click или Shift+Click)
4. Все фото загрузятся одновременно
5. При необходимости удалите ненужные
6. Добавьте комментарий (если требуется)
7. Нажмите "Завершить задачу"

## Технические детали

### Логика массового закрытия

```typescript
// Псевдокод
for each task in taskIds:
  if task.status === 'COMPLETED':
    skip (already completed)
  
  if task.object.requirePhotoForCompletion:
    skip (needs photo)
  
  if task.object.requireCommentForCompletion:
    skip (needs comment)
  
  if task.isVirtual:
    materialize task
  
  complete task
```

### Материализация виртуальных задач

Виртуальные задачи существуют только в памяти до момента их завершения. При закрытии:

1. Создается реальная запись `Task` в БД
2. Копируются все данные из `TaskExecution`
3. Добавляются данные о завершении
4. Сохраняются фотографии
5. Возвращается ID реальной задачи

### Индикаторы требований

Требования берутся из полей объекта:
- `requirePhotoForCompletion: boolean`
- `requireCommentForCompletion: boolean`

Эти поля устанавливаются в настройках объекта.

## API Endpoints

### Массовое закрытие
```
POST /api/tasks/bulk-complete
Body: {
  taskIds: string[],
  skipValidation?: boolean
}
Response: {
  message: string,
  completed: number,
  skipped: number,
  skippedTasks: Array<{id, title, reason}>
}
```

### Завершение задачи
```
POST /api/tasks/unified-complete
Body: {
  taskId: string,
  completionNote?: string,
  completionPhotos?: string[]
}
Response: {
  success: boolean,
  task: Task
}
```

### Загрузка фото
```
POST /api/photos/upload
Body: FormData with 'file'
Response: {
  url: string,
  id: string
}
```

### Удаление фото
```
DELETE /api/photos/{id}
Response: {
  success: boolean
}
```

## Совместимость

- ✅ Все существующие задачи работают как раньше
- ✅ Объекты без требований закрываются быстро
- ✅ Объекты с требованиями требуют фото/комментарий
- ✅ Виртуальные задачи материализуются автоматически
- ✅ Обратная совместимость полностью сохранена

## Источники изменений

### Коммит 1: Массовое закрытие и индикаторы
```
commit 3234b72bf4c703934e9b3e25be73ffda787f9470
Author: olgakycenko <olgakucenko97@gmail.com>
Date:   Fri Nov 21 10:51:13 2025 +0300

    Фикс массового закрытия задач и индикаторов требований
```

**Изменения:**
- Добавлен API массового закрытия
- Обновлен SimpleTaskListModal
- Добавлены индикаторы в ObjectCard
- Улучшен unified-task-system

### Коммит 2: Фотографии и UX
```
commit e9f87b59bef10232da2449e265aa404036882675
Author: olgakycenko <olgakucenko97@gmail.com>
Date:   Tue Nov 18 08:50:04 2025 +0300

    Fix calendar task completion photos, photo reports and modal UX
```

**Изменения:**
- Улучшена загрузка фотографий
- Обновлен UnifiedTaskCompletionModal
- Исправлены API для фото
- Улучшен UX завершения задач

## Тестирование

### Тест 1: Массовое закрытие без требований

1. Создайте объект без требований
2. Создайте несколько задач на сегодня
3. Откройте календарь
4. Нажмите "Закрыть все задачи"
5. ✅ Все задачи должны закрыться

### Тест 2: Массовое закрытие с требованиями

1. Создайте объект с требованием фото
2. Создайте несколько задач на сегодня
3. Откройте календарь
4. Нажмите "Закрыть все задачи"
5. ✅ Задачи должны быть пропущены
6. ✅ Должно показаться сообщение о пропущенных задачах

### Тест 3: Выборочное закрытие

1. Откройте список задач
2. Нажмите "Выбрать задачи"
3. Выберите 3 задачи
4. Нажмите "Закрыть выбранные (3)"
5. ✅ Должны закрыться только выбранные задачи

### Тест 4: Индикаторы требований

1. Создайте объект с требованием фото
2. Откройте календарь
3. ✅ На карточке должна быть оранжевая метка с камерой
4. ✅ Метка должна пульсировать

### Тест 5: Множественная загрузка фото

1. Откройте задачу для завершения
2. Нажмите "Выбрать фото"
3. Выберите 3 фото
4. ✅ Все 3 фото должны загрузиться
5. ✅ Должен быть предпросмотр всех фото
6. Удалите одно фото
7. ✅ Должно остаться 2 фото

## Следующие шаги (опционально)

1. Добавить фильтрацию по требованиям в календаре
2. Добавить статистику по массовому закрытию
3. Добавить историю массовых операций
4. Добавить возможность отменить массовое закрытие
5. Добавить экспорт задач с требованиями

---

**Статус:** ✅ Все изменения применены и готовы к использованию
