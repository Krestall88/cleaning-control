# Обновление дашборда и системы инвентаря

## Дата обновления
26 ноября 2025

## Обзор изменений

Добавлена расширенная функциональность дашборда, системы инвентаря и отчетности по расходам из проекта toplevel.

## Новые компоненты

### Дашборд

1. **ModernDashboard.tsx** - Современный дашборд с аналитикой
   - Упрощенный блок аналитики по инвентарю
   - Интеграция модальных окон для детальной статистики
   - Real-time активность
   - Топ менеджеры и объекты

2. **TasksStatsModal.tsx** - Модальное окно статистики задач
   - Количественные показатели по всем типам задач
   - Графики: столбчатая и круговая диаграммы
   - Детализация по календарю, доп. задачам и чек-листам

3. **CompletionStatsModal.tsx** - Модальное окно статистики выполнения
   - Процентные показатели выполнения
   - Графики: линейный тренд за 7 дней и радиальная диаграмма
   - Сводная информация по лучшим/худшим показателям

### Инвентарь и расходы

1. **InventoryFinancialReport.tsx** - Финансовый отчет по инвентарю
   - Вкладки: Общее, По статьям, По объектам
   - Визуальное различие типов лимитов (годовые, полугодовые, месячные, дневные)
   - Интеграция с графиками расходов
   - Управление лимитами и расходами

2. **ExpenseCategoriesManager.tsx** - Управление категориями расходов
   - CRUD операции для категорий
   - Привязка к объектам

3. **ExpenseCategoryStatsCard.tsx** - Карточка статистики по категории
   - Отображение лимитов и расходов
   - Прогресс-бары

4. **ExpenseChart.tsx** - График расходов
   - Визуализация трендов

5. **ExpenseChartModal.tsx** - Модальное окно с детальным графиком
   - Расширенная аналитика по расходам
   - Информация об объекте

6. **ObjectExpenseLimitsSettings.tsx** - Настройки лимитов для объекта
   - Управление лимитами по категориям
   - Поддержка разных типов периодов (день, месяц, полгода, год)

## API Endpoints

### Dashboard
- `GET /api/dashboard/modern` - Данные для современного дашборда
- `GET /api/dashboard/tasks-stats` - Статистика задач
- `GET /api/dashboard/completion-stats` - Статистика выполнения

### Inventory
- `GET /api/inventory` - Список инвентаря
- `GET /api/inventory/[id]` - Детали инвентаря
- `GET /api/inventory/financial-report` - Финансовый отчет
- `GET /api/inventory/chart-data` - Данные для графиков
- `POST /api/inventory/expenses` - Создание расхода
- `PUT /api/inventory/expenses/[id]` - Обновление расхода
- `DELETE /api/inventory/expenses/[id]` - Удаление расхода
- `GET /api/inventory/limits` - Список лимитов
- `POST /api/inventory/bulk-limits` - Массовое создание лимитов

### Expense Categories
- `GET /api/expense-categories` - Список категорий
- `POST /api/expense-categories` - Создание категории
- `PUT /api/expense-categories/[id]` - Обновление категории
- `DELETE /api/expense-categories/[id]` - Удаление категории

### Expense Limits
- `GET /api/objects/[id]/expense-limits` - Лимиты объекта
- `POST /api/objects/[id]/expense-limits` - Создание лимита
- `PUT /api/expense-limits/[id]` - Обновление лимита
- `DELETE /api/expense-limits/[id]` - Удаление лимита
- `POST /api/expense-limits/cleanup-duplicates` - Очистка дубликатов

### Expenses
- `GET /api/expenses` - Список расходов
- `POST /api/expenses` - Создание расхода
- `PUT /api/expenses/[id]` - Обновление расхода
- `DELETE /api/expenses/[id]` - Удаление расхода
- `GET /api/expenses/stats/[objectId]` - Статистика по объекту
- `GET /api/objects/[id]/expenses` - Расходы объекта

## Ключевые особенности

### Типы лимитов
Система поддерживает 4 типа периодов для лимитов:
- **DAILY** - дневной лимит (умножается на количество дней в месяце)
- **MONTHLY** - месячный лимит (используется как есть)
- **SEMI_ANNUAL** - полугодовой лимит (делится на 6)
- **ANNUAL** - годовой лимит (делится на 12)

### Визуальное различие лимитов
- Годовые и полугодовые лимиты отображаются в синей рамке
- Показываются две цифры: общая сумма и месячный эквивалент
- Месячные и дневные лимиты - стандартное отображение

### Статусы задач
Система корректно учитывает все статусы:
- **Task**: COMPLETED, CLOSED_WITH_PHOTO считаются завершенными
- **AdditionalTask**: только COMPLETED
- **Checklist**: наличие completedAt

### Фильтрация по ролям
- **ADMIN** - видит все данные
- **MANAGER** - видит только свои объекты
- **ACCOUNTANT** - полный доступ к инвентарю и расходам

## Страницы

- `/inventory` - Главная страница инвентаря с финансовым отчетом

## Зависимости

Убедитесь, что установлены следующие пакеты:
- `recharts` - для графиков
- `lucide-react` - для иконок
- `@radix-ui/react-dialog` - для модальных окон
- `@radix-ui/react-tabs` - для вкладок
- `date-fns` - для работы с датами

## Миграции базы данных

Необходимо убедиться, что в схеме Prisma присутствуют следующие модели:
- `ExpenseCategory`
- `ExpenseCategoryLimit`
- `InventoryExpense`
- `CleaningObject`
- `Task`
- `AdditionalTask`
- `Checklist`

## Использование

### Дашборд
```tsx
import ModernDashboard from '@/components/ModernDashboard';

<ModernDashboard userRole={user.role} userId={user.id} />
```

### Инвентарь
Перейдите на `/inventory` для доступа к финансовому отчету.

## Примечания

1. Все API endpoints поддерживают аутентификацию через JWT токены
2. Данные кэшируются на стороне клиента для оптимизации
3. Модальные окна обновляют данные при открытии
4. Графики адаптивны и работают на мобильных устройствах

## Логирование

API endpoints выводят детальную информацию в консоль сервера:
- 📊 Tasks Stats - статистика задач
- 📈 Completion Stats - статистика выполнения

Используйте эти логи для отладки несоответствий в данных.
