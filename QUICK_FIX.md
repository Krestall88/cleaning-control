# 🚀 Быстрое исправление проблем с запуском

## ✅ Проблема решена!

Сервер был перезапущен и очищен кэш Next.js.

## 🔧 Что было сделано:

1. **Остановлен старый процесс Node.js**
2. **Удалена папка `.next`** (кэш сборки)
3. **Запущен dev сервер заново**

## 🌐 Сервер работает:

- **Локальный URL**: http://localhost:3000
- **Сетевой URL**: http://192.168.1.106:3000

## 📝 Что делать дальше:

### 1. Откройте браузер

Перейдите по адресу: http://localhost:3000/auth/login

### 2. Войдите в систему

Используйте учетные данные из базы данных.

**Если не помните пароль**, создайте нового пользователя:

```bash
npm run seed
```

Это создаст тестовых пользователей:
- **Админ**: admin@example.com / admin123
- **Менеджер**: manager@example.com / manager123

### 3. Проверьте инструкции

После входа инструкции должны загружаться корректно.

## ❗ Если проблемы остаются:

### Проблема: 404 на `/api/auth/login`

**Решение**: Очистите кэш браузера (Ctrl+Shift+Del) и перезагрузите страницу (Ctrl+F5)

### Проблема: 500 на `/api/manual/*`

**Причина**: Отсутствуют данные в таблице `ManualSection`

**Решение**: Добавьте разделы инструкций в базу данных:

```sql
-- Выполните в Supabase SQL Editor
INSERT INTO "ManualSection" (id, slug, title, icon, "order", content, "createdAt", "updatedAt")
VALUES 
  (gen_random_uuid(), 'getting-started', 'Начало работы', '🚀', 1, '# Начало работы\n\nДобро пожаловать в систему управления уборкой!', NOW(), NOW()),
  (gen_random_uuid(), 'objects', 'Объекты', '🏢', 2, '# Управление объектами\n\nКак работать с объектами уборки.', NOW(), NOW()),
  (gen_random_uuid(), 'tasks', 'Задачи', '✅', 3, '# Управление задачами\n\nКак создавать и выполнять задачи.', NOW(), NOW());
```

### Проблема: Инструкции не отображаются

**Решение**: Проверьте таблицу `ManualScreenshot`:

```sql
-- Проверьте наличие скриншотов
SELECT * FROM "ManualScreenshot" LIMIT 5;

-- Если пусто, добавьте тестовый скриншот
INSERT INTO "ManualScreenshot" (id, title, description, url, "sectionId", "order", "createdAt", "updatedAt")
VALUES (
  gen_random_uuid(), 
  'Главная страница', 
  'Пример главной страницы системы', 
  'https://via.placeholder.com/800x600', 
  (SELECT id FROM "ManualSection" WHERE slug = 'getting-started' LIMIT 1),
  1,
  NOW(),
  NOW()
);
```

## 🔄 Перезапуск сервера в будущем:

Если нужно перезапустить сервер:

```bash
# Остановить (Ctrl+C в терминале где запущен npm run dev)
# Или принудительно:
Stop-Process -Name node -Force

# Очистить кэш (опционально, если есть проблемы)
Remove-Item -Path .next -Recurse -Force

# Запустить заново
npm run dev
```

## 📚 Полезные команды:

```bash
# Проверить запущенные Node процессы
Get-Process | Where-Object {$_.ProcessName -like "*node*"}

# Проверить порт 3000
netstat -ano | findstr :3000

# Остановить процесс по ID
Stop-Process -Id <PID> -Force

# Посмотреть логи Prisma
npx prisma studio

# Проверить подключение к БД
npx prisma db pull
```

## 🎯 Следующие шаги:

1. ✅ Войдите в систему
2. ✅ Проверьте работу инструкций
3. ✅ Протестируйте загрузку фото (из предыдущего фикса)
4. ✅ Проверьте привязку Telegram ID

---

**Сервер запущен и готов к работе!** 🎉
